// AUTO-GENERATED by tools/generate-verb-node-overrides-schema-sidecar.js — do not edit by hand.
// Sidecar wrapper so browser tooling can load the schema under file://
// (bypasses Chromium CORS fetch block). Keep in sync with
// tools/verb-node-overrides-schema.json. Consumed via window.VERB_NODE_OVERRIDES_SCHEMA.
window.VERB_NODE_OVERRIDES_SCHEMA = {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dcgamejam2026/schemas/verb-node-overrides-schema.json",
  "title": "VerbNode Overrides Schema (per-floor tweaks to procgen dungeon node derivation)",
  "description": "DOC-110 P3 Ch.2 stretch. Per-floor override file for depth>=3 dungeon floors. The auto-derivation in engine/dungeon-verb-nodes.js scans the tile grid and derives spatial verb-nodes; this override layer lets authors append custom nodes, remove specific auto-derived ids, or mutate fields (type/faction/contested) on individual nodes — all without hand-editing data/verb-nodes.json (which is reserved for hand-authored depth 1-2 floors). One file per floor under tools/verb-node-overrides/{floorId}.json. Filename must match _meta.floorId. Bundled into data/verb-node-overrides.js by tools/extract-verb-node-overrides.js and applied at DungeonVerbNodes.populate() time via engine/verb-node-overrides-seed.js.",
  "definitions": {
    "gridCoord": {
      "type": "integer",
      "minimum": 0,
      "maximum": 255
    },
    "floorId": {
      "type": "string",
      "pattern": "^[0-9]+(\\.[0-9]+)*$",
      "description": "Dotted depth path (CLAUDE.md §Floor hierarchy). Stretch scope is depth>=3 only but the schema does not enforce — the runtime ignores overrides for floors DungeonVerbNodes never visits."
    },
    "nodeType": {
      "type": "string",
      "enum": [
        "bonfire",
        "well",
        "bench",
        "shop_entrance",
        "bulletin_board",
        "faction_post",
        "work_station",
        "rest_spot",
        "soup_kitchen"
      ]
    },
    "faction": {
      "type": "string",
      "enum": [
        "tide",
        "foundry",
        "admiralty",
        "pinkerton",
        "jesuit",
        "bprd",
        "mss"
      ]
    },
    "nodeDef": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "type",
        "x",
        "y"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "$ref": "#/definitions/nodeType"
        },
        "x": {
          "$ref": "#/definitions/gridCoord"
        },
        "y": {
          "$ref": "#/definitions/gridCoord"
        },
        "faction": {
          "$ref": "#/definitions/faction"
        },
        "contested": {
          "type": "boolean"
        }
      }
    },
    "replacePatch": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "patch"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Node id to mutate. Must match an auto-derived id (e.g. dvn_2.2.1_rest_4_4) or a node already added by an earlier override."
        },
        "patch": {
          "type": "object",
          "additionalProperties": false,
          "description": "Fields to overwrite. Coordinates (x/y) are intentionally excluded — to move a node, remove + add.",
          "properties": {
            "type": {
              "$ref": "#/definitions/nodeType"
            },
            "faction": {
              "$ref": "#/definitions/faction"
            },
            "contested": {
              "type": "boolean"
            }
          },
          "minProperties": 1
        }
      }
    }
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "floorId"
  ],
  "properties": {
    "_meta": {
      "type": "object",
      "description": "Optional authoring metadata. Regenerated by tools/extract-verb-node-overrides.js."
    },
    "floorId": {
      "$ref": "#/definitions/floorId"
    },
    "add": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/nodeDef"
      },
      "description": "Nodes appended after the DungeonVerbNodes auto-scan. Ids must be unique within the floor (across auto-derived + overrides)."
    },
    "remove": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Node ids to drop from the auto-derived list. Silent no-op if the id was never derived."
    },
    "replace": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/replacePatch"
      },
      "description": "Field-level mutations on existing nodes. Applied after removes but before adds."
    }
  }
};
